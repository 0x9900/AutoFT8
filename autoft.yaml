#
# AutoFT configuration file
#

mongo_server: "localhost"
bind_address: "127.0.0.1"
wsjt_port: 2238
monitor_port: 2240
call: "W6BSD"
location: "CM87vl"
max_tries: 5                    # It's better to leave it at 5
select_method: "grid.NotGrid"   # plugin name: <module.name>.<class>

# Plugins configurations
# Select call from the follwing grid squares.
Grid:
  squares:
    - ^[CDEF][NM]
    - ^EL

# Select calls outside the following grid squares.
NotGrid:
  squares:
    - ^[CDEF][NM]
    - ^EL

# uk grid
#  - ^I[NO]
#  - ^J[MNOP]
#  - ^K[MNOP]
#  - ^L[MN]
#
# us grid
#  - ^[CDEF]N
#  - ^[CDEF]M
#  - ^EL
